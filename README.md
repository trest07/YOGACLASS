# YogaClass

## Setup
```bash
git clone https://github.com/trest07/YOGACLASS.git
cd YOGACLASS
cp .env.example .env   # Fill in real values
npm install
npx supabase db push --db-url "url_de_tu_prod"
npm run dev
